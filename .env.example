# Copiază acest fișier ca .env și completează valorile.
# Nu comite .env în git (conține parole).

# ——— Baza de date PostgreSQL ———
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ProconixDB
DB_USER=postgres
# Obligatoriu pe VPS dacă PostgreSQL folosește parolă (SCRAM). Lăsat gol → erori de tip SASL / „client password must be a string”.
DB_PASSWORD=your_postgres_password_here

# Variante alternative (libpq): PGHOST, PGPORT, PGDATABASE, PGUSER, PGPASSWORD

# După deploy (local sau VPS), aliniază schema PostgreSQL cu aplicația:
#   npm run db:bootstrap
# Opțional, creează baza dacă lipsește:
#   BOOTSTRAP_CREATE_DB=1 npm run db:bootstrap
# Verificare tabele: npm run db:verify

# ——— Server ———
# PORT=3000
# HOST=localhost

# ——— Email (formular „Request a Callback” pe see_plans.html) ———
# Mesajele (See plans „Request a Callback” + Contact Us) merg la această adresă:
# CALLBACK_NOTIFY_EMAIL=info@proconix.uk
# Formular „Book a demo” pe index.html (implicit info@proconix.uk):
# BOOK_DEMO_NOTIFY_EMAIL=info@proconix.uk
# Copie BCC: bun venit după înregistrare + email când contul manager e activat de admin (implicit rdemian732@gmail.com; omis dacă = adresa managerului):
# COMPANY_WELCOME_BCC_EMAIL=rdemian732@gmail.com
# SMTP pentru trimitere (ex. furnizor de mail, Office 365, SendGrid SMTP):
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# Adresa „From” vizibilă (spf/dkim trebuie să permită serverul tău):
# SMTP_FROM=noreply@proconix.uk
